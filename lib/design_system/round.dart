import 'package:flutter/material.dart';

class AppRadius {
  static const BorderRadius smallRound = BorderRadius.all(Radius.circular(8));
  static const BorderRadius mediumRound = BorderRadius.all(Radius.circular(12));
  static const BorderRadius navRound = BorderRadius.all(Radius.circular(16));
}
